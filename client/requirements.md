## Packages
framer-motion | For smooth page transitions and micro-interactions
date-fns | for date formatting
react-day-picker | for calendar components

## Notes
- "Custom Locations" are managed via a dedicated settings/locations page.
- Properties have a "status" field that drives visual indicators (shortlisted, visited, offered, rejected).
- Distances are manually input for now, but the UI should look like it could be automated later.
